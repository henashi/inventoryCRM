spring:
  cloud:
    nacos:
      serverAddr: 127.0.0.1:8848
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:nacos}
  config:
    import:
      - nacos:test.yml?refreshEnabled=true&group=DEFAULT_GROUP
